[pytest]
# Enable modern asyncio support for async tests
asyncio_mode = auto

# Look for tests in the top-level tests package
testpaths = tests

# Verbose test names; -s shows print() output so API results are visible
addopts = -v -s

